# Problem: Separate Local Dev from Cloud Deploy

Open:

```text
workspace-blueprints/local-workflow.md
```

Create two lists:

1. What local development can prove.
2. What only deploy review or AWS-side testing can prove.

Then answer:

1. Why does local success not prove IAM safety?
2. Why should deployment require controlled credentials?

Completion rule: you can state which risks remain after local development passes.
