# Problem: Plan Full-Stack Generator Sequence

Open:

```text
workspace-blueprints/generator-map.md
scenario-designs/fullstack-cdk.yml
```

Write a generator sequence for a TypeScript website, Cognito auth, tRPC API, CDK infrastructure, and frontend-to-backend connection.

Then answer:

1. Which command creates the workspace?
2. Which commands create app projects?
3. Which command adds infrastructure?
4. Which command connects projects?
5. Where would you add `--dry-run` during exploration?

Completion rule: you can order generators so project references exist before they are connected.
