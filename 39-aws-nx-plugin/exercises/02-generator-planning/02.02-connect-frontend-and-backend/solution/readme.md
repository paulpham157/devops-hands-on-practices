# Solution: Connect Frontend and Backend

The source project is the website. The target project is the API.

A generated connection is better than a pasted endpoint because it keeps the relationship inside the workspace model and can support local and deployed environments more consistently.

After generation, test that the frontend can call the API locally and that generated types or clients compile. Before deployment, review auth behavior, endpoint exposure, environment variables, and generated infrastructure references.
