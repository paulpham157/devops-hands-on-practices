# Claude Instructions

Read `AGENTS.md` in this directory first. It is the source of truth for repository structure, validation commands, naming conventions, commit guidance, and security rules.

Do not duplicate contributor rules here; keep this file as a pointer so agent behavior stays consistent when `AGENTS.md` changes.
