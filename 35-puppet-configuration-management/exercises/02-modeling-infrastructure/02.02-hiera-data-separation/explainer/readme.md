# Hiera Data Separation

Hiera keeps changing values out of manifests so the code stays stable while environments differ.

Inspect:

- `hiera.yaml`
- `data/common.yaml`
- `data/environments/production.yaml`
