# Classes, Profiles, and Roles

You need three node types: `docker-host`, `ci-runner`, and `artifact-cache`.

Write a proposed breakdown:

- Which shared profile belongs on all three?
- Which profile is unique to `ci-runner`?
- Which role class should be assigned directly to nodes?
