# Code Manager and Puppetfile

A change introduces `puppetlabs-apt` and updates `role::docker_host`.

Answer these questions:

1. Why should module source declarations live in `Puppetfile` instead of ad hoc install notes?
2. What is the safer promotion order: edit production first, or promote tested code through environments?
3. Where would you document any remaining legacy `r10k` behavior during a migration?
