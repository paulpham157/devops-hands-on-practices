# Code Manager and Puppetfile

Modern Puppet Enterprise teams commonly deploy code with Code Manager and use a `Puppetfile` to declare external module dependencies.

Read:

- `Puppetfile`
- the environment notes in `README.md`
