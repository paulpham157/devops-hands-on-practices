# Claude Instructions

Read `AGENTS.md` in this lesson directory first, then read the root `../AGENTS.md` if broader repository guidance is needed.

Treat `21-aws-azure-gcp-cloud-fundamentals/AGENTS.md` as the source of truth for lesson-specific structure, commands, validation, and editing rules. Do not duplicate those rules here.
