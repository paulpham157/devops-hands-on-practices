# Solution: Compare Cloud Providers

AWS EC2 is closest to Azure Virtual Machines.

Google Cloud Storage is closest to S3.

Managed Kubernetes services:

- AWS EKS
- Azure AKS
- Google GKE

Azure uses Resource Groups as an organizing unit.

Service mapping is useful because the same architecture concepts appear across providers under different product names.

