# Explainer: Map Compute, Storage, and Networking

Most cloud architectures combine a few core categories:

- compute runs code.
- storage keeps files, objects, or blocks.
- databases store structured data.
- networking connects and isolates resources.
- IAM controls access.
- observability shows behavior.

Provider product names differ, but the categories repeat.

