# User Journeys and Good Events

Open `sli-models/checkout-sli.json` and `sli-models/search-sli.yml`.

Answer:

1. What is the checkout user journey?
2. What counts as a bad event for checkout?
3. Why is an empty search result not always a bad event?
4. Why are user journeys a better starting point than only CPU or memory metrics?

