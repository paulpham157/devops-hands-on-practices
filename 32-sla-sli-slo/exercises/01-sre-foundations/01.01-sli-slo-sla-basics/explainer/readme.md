# SLI, SLO, SLA Basics

These three terms are related but not interchangeable.

- `SLI` is a measurement
- `SLO` is a target for that measurement
- `SLA` is a promise with consequences

Most confusion comes from treating them as the same artifact.

