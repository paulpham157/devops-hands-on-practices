# Add Port Validation

Variable validation catches unsafe or impossible inputs before planning reaches provider operations.

For ports, validation can reject values outside the valid TCP/UDP port range or values reserved for other local services.
