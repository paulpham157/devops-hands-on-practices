# Problem: Understand Inferred Tasks

Open:

```text
plugin-catalog/plugin-selection.md
workspace-maps/command-map.md
```

Answer:

1. What is an inferred task?
2. Why can inferred tasks reduce `project.json` boilerplate?
3. Which plugin families are likely to infer tasks from tool config?
4. What should you inspect after adding a plugin?

Completion rule: you can explain inferred tasks without assuming they are magic.
