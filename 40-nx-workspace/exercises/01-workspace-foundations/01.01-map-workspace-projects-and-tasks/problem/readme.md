# Problem: Map Workspace Projects and Tasks

Open:

```text
README.md
workspace-maps/command-map.md
```

For a repo with `web`, `api`, `shared-ui`, and `web-e2e`, answer:

1. Which entries are likely projects?
2. Which targets might each project expose?
3. Which commands inspect projects before changing files?
4. Why should this mapping happen before plugin selection?

Completion rule: you can separate project boundaries from task names.
