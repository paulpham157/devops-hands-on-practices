# Explainer: Map Workspace Projects and Tasks

An Nx workspace is made of projects and tasks.

A project can be an app, library, package, service, e2e project, or infrastructure project. A task is named work that can run for a project, such as `build`, `test`, `lint`, or `e2e`.

Good Nx adoption starts by naming these boundaries before adding plugins or generators.
