# Chef Control Plane Roles

| Task | Best First Component |
| --- | --- |
| Generate a new cookbook and run local tests | Chef Workstation |
| Store policy for many managed nodes | Chef Infra Server |
| Apply the policy on a target server | chef-client |

Local mode is useful for fast iteration, lab work, and validating cookbook behavior before wiring it into a shared server workflow.
