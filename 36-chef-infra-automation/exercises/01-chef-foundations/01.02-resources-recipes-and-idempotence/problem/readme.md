# Resources, Recipes, and Idempotence

Read `cookbooks/base/recipes/default.rb` and `cookbooks/docker_host/recipes/default.rb`.

Then answer:

1. Which recipe establishes the shared baseline?
2. Which resource would be most likely to trigger a service restart if the team later adds notifications?
3. Why is using a template better than writing the file with raw shell commands?
