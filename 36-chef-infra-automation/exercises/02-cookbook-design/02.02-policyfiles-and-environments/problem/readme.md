# Policyfiles and Environments

A teammate changes the `docker_host` cookbook but forgets to refresh the lock before promotion.

1. What risk does that create?
2. Why is committing `Policyfile.lock.json` useful in review?
3. Which file should operators inspect first to confirm the active run list?
