# Cookbooks, Templates, and Attributes

The platform team wants staging to use `journald` and production to keep `json-file`.

Answer:

1. Which file should own the default value?
2. Which file renders the final daemon config?
3. Where should environment-specific overrides go if the team uses Policyfiles?
