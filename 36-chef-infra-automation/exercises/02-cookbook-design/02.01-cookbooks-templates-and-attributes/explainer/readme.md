# Cookbooks, Templates, and Attributes

Modern Chef code keeps behavior in recipes and variable values in attributes or policy data.

Inspect:

- `cookbooks/docker_host/attributes/default.rb`
- `cookbooks/docker_host/templates/default/daemon.json.erb`
