# Test Kitchen and ChefSpec

Read `.kitchen.yml` and `test/integration/default/controls/default.rb`.

Then answer:

1. Which tool converges the instance?
2. Which tool verifies that `/etc/docker/daemon.json` exists?
3. Why should teams keep at least one verification control close to the cookbook?
