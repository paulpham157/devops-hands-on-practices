# Chef Governance Model

Define a governance outline for a platform with 12 service teams.

Include:

1. Who approves changes to the shared base cookbook?
2. Which checks must pass before updating `Policyfile.lock.json`?
3. Which changes should require staging verification before production promotion?
