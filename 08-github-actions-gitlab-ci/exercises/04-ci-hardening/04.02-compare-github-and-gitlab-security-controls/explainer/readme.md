# Compare GitHub and GitLab Security Controls

GitHub Actions and GitLab CI solve similar problems, but the control points are not identical.

When hardening CI, compare permissions, secret exposure rules, runner trust, artifact retention, and branch or environment protections.
