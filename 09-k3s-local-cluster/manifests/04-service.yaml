apiVersion: v1
kind: Service
metadata:
  name: k3s-demo
  namespace: docker-course-k3s
  labels:
    app.kubernetes.io/name: k3s-demo
    app.kubernetes.io/part-of: docker-course
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: k3s-demo
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
