# Debug Rollout Events and Pod Status

## Task

Assume a rollout did not finish cleanly. Which commands would you run first, and what would each one tell you?

Include at least:

- `kubectl rollout status`
- `kubectl get pods`
- `kubectl describe pod`
- `kubectl get events`

## Done When

You can explain a debugging order that narrows the failure before diving into raw logs.
