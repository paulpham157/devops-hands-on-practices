# Solution: Map the Lab

Expected answers:

1. Optional VM setup: `examples/lab-setup`
2. Simple image examples: `examples/docker-image-examples`
3. Single-stage, multi-stage, and distroless builds: `examples/multi-stage-builds`
4. Layer optimization: `examples/optimize-docker-images`
5. Dockerfile review notes: `notes/dockerfile-review-checklist.md`

Expected validation:

```text
lesson 00 structure ok
```
