# Problem: Map the Lab

From `00-docker-fundamentals`, inspect the folders:

```bash
find . -maxdepth 2 -type d | sort
```

Create a short note for yourself answering:

1. Which folder is for optional VM setup?
2. Which folder contains simple image examples?
3. Which folder compares single-stage, multi-stage, and distroless builds?
4. Which folder is about Docker layer optimization?
5. Which file should you read before reviewing Dockerfiles?

Verify the lesson structure:

```bash
./scripts/validate.sh
```
