# Problem: Message-Driven AsyncAPI

Open:

```text
protocol-specs/asyncapi.yaml
```

Answer:

1. What is the event channel address?
2. What is the message name?
3. Which fields are required in the payload?
4. Why should consumers be idempotent?
5. When is async messaging better than a direct REST call?

