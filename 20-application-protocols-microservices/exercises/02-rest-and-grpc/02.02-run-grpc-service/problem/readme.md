# Problem: Run gRPC Service

Run:

```bash
docker compose up --build
./scripts/call-grpc.sh
```

Answer:

1. What port does gRPC expose on the host?
2. Which method returns a product?
3. Which message contains the product ID request?
4. Which generated files are created during Docker build?
5. Why is gRPC less convenient to call manually than REST?

