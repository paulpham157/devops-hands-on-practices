# Problem: Run REST API

Run:

```bash
docker compose up --build
./scripts/call-rest.sh
```

Answer:

1. What URL returns product `42`?
2. What content format does REST return?
3. Which field says the protocol is REST?
4. Which file documents the REST contract?
5. What would be a reasonable REST URL for listing products?

