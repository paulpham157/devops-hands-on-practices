# Solution: Run REST API

The product URL is:

```text
http://localhost:5200/products/42
```

REST returns JSON in this lesson.

The `protocol` field says:

```text
rest
```

The contract file is:

```text
rest-api/openapi.yaml
```

A reasonable list URL would be:

```text
GET /products
```

