# Explainer: Run REST API

REST APIs expose resources over HTTP.

In this lesson:

```text
GET /products/42
```

returns JSON.

REST works well with standard HTTP tools, proxies, caches, and gateways.

OpenAPI documents the resource, parameters, response schemas, and status codes.

