# Aggregation, Transformation, and Auth

BFF often combines:

- aggregation from several domain services
- field shaping for one client contract
- auth or session adaptation for that client
- downstream error normalization

This is orchestration for presentation, not ownership of business truth.

