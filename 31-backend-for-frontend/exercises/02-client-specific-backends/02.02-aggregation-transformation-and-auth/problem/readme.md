# Aggregation, Transformation, and Auth

Open `bff-shared-services/aggregation-flow.yml` and `bff-shared-services/auth-and-cache-notes.md`.

Answer:

1. Which downstream services are called in the example flow?
2. Name two BFF behaviors listed in the flow.
3. What client-edge concerns are called out in the notes?
4. Why should business rules stay in downstream domain services?

