# Problem: Portable Handler Shapes

Inspect:

```text
function-examples/
```

Answer:

1. Which examples return `{ statusCode, body }`?
2. Which example exports `fetch(request)`?
3. Which example uses `context`?
4. Which examples use request/response objects?
5. Why separate business logic from platform adapter code?

