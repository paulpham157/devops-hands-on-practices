# Problem: Network and Ports

Inside Ubuntu or Alpine, run:

```bash
./scripts/network-demo.sh
```

Answer:

1. What is the container hostname?
2. Is the `ip` command available?
3. Is the `ss` command available?
4. What does a listening port mean?
5. Why does binding to `127.0.0.1` behave differently from binding to `0.0.0.0` in containers?

