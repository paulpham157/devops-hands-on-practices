# Build Chains and Artifact Dependencies

Read `build-configs/build-chain.md`, `ci-cd/artifact-contract.txt`, and `settings.kts`.

Then answer:

1. Which build publishes `reports/build-metadata.txt`?
2. Which build consumes it through an artifact dependency?
3. Why is artifact flow useful even when the upstream build already passed?
