# Kotlin DSL and Templates

A team has 18 services with the same Docker test-and-build pattern.

Answer:

1. Why is Kotlin DSL better than copying UI configs 18 times?
2. Which part of the sample should become a reusable template first?
3. What review benefit comes from keeping pipeline changes in Git?
