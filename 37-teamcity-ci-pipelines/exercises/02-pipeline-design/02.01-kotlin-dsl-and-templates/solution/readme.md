# Kotlin DSL and Templates

1. Kotlin DSL reduces drift and makes bulk changes repeatable.
2. The shared test and container build step structure should become a reusable template first.
3. Teams can code-review branch filters, parameters, triggers, and dependencies like any other change.
