# Branch Policies and Merge Gates

1. Only `main` and `release-*` should publish release artifacts.
2. Feature branches need early validation so problems are caught before merge.
3. The pull request should receive build status for test, packaging, and any required quality gates.
