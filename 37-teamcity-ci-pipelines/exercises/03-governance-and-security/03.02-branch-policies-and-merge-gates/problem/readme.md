# Branch Policies and Merge Gates

Read `build-configs/branch-policy.md`.

Then answer:

1. Which branches should be allowed to publish release artifacts?
2. Why should `feature-*` branches still run builds even if they never publish?
3. What feedback should reach the pull request before merge?
