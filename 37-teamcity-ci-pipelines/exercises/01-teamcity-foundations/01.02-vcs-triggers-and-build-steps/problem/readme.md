# VCS Triggers and Build Steps

Read `ci-cd/.teamcity/settings.kts`.

Then answer:

1. Which branches are allowed by the build VCS branch filter?
2. Which branches trigger `Build and Test` automatically?
3. Why is it useful to allow more branches than you auto-trigger?
