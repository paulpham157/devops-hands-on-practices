# VCS Triggers and Build Steps

1. `main`, `release-*`, and `feature-*` are allowed for the build.
2. Automatic VCS trigger coverage is configured for `main` and `feature-*`.
3. It allows manual or downstream execution on selected branches without creating noisy automatic builds everywhere.
