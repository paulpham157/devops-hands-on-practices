# Nova, Neutron, Cinder, and Glance

Based on `heat/instance-stack.yml`, answer:

1. Which resource controls inbound SSH and HTTP policy?
2. Which resource gives the VM a routable public address?
3. Why is an explicit port more practical than only naming a network in richer designs?
