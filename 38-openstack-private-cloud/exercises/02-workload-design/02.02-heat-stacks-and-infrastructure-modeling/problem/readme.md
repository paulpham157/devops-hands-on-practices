# Heat Stacks and Infrastructure Modeling

Read both files under `heat/`.

Then answer:

1. Which template owns the router and subnet wiring?
2. Which template owns the server and floating IP path?
3. Why are outputs useful in day-two operations?
