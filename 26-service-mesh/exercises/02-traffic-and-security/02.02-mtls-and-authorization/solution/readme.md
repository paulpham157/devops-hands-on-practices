# mTLS and Authorization

1. `PeerAuthentication`.
2. `STRICT`.
3. `AuthorizationPolicy`.
4. `Server`.
5. `ServiceIntentions`.

The common pattern is the same across implementations: establish identity first, then express which identities may talk to which services.

