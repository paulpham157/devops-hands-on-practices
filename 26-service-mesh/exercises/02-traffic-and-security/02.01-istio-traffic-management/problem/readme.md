# Istio Traffic Management

Open `istio/virtual-service.yaml` and `istio/destination-rule.yaml`.

Answer:

1. Which subset gets 90 percent of traffic?
2. Which subset gets 10 percent of traffic?
3. Which object defines the subset labels?
4. What retry count is configured?
5. Why is weighted routing useful during a canary rollout?

