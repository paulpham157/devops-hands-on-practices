# Istio Traffic Management

Istio traffic policy often combines:

- `VirtualService` for request routing rules
- `DestinationRule` for subsets and traffic policy

This model allows teams to separate service versions from traffic percentages.

