# Observability and Failure Handling

Use `platform-maps/operations-checklist.md` and `istio/virtual-service.yaml`.

Answer:

1. Which two failure-handling settings appear in the Istio traffic policy?
2. Why should retries be configured carefully rather than maximized?
3. What extra observability value can a mesh provide over application-only metrics?
4. Why must the team learn to debug proxies as part of incident response?

