# Problem: Compare Repository Modes

Classify each repository as hosted/local, proxy/remote, or group/virtual:

1. `docker-hosted`
2. `dockerhub-proxy`
3. `docker-group`
4. `helm-local`
5. `helm-remote`
6. `helm-virtual`
7. `maven-releases`
8. `npmjs-remote`

Then answer:

1. Which mode should CI publish internal artifacts to?
2. Which mode should developer laptops usually consume from?
3. Which mode protects you from repeated public internet downloads?
4. Why should prod deployments avoid mutable tags?

