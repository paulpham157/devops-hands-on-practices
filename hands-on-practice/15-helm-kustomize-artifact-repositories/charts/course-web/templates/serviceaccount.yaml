{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "course-web.serviceAccountName" . }}
  labels:
    {{- include "course-web.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
