# Read a CloudFormation Template

Open `cloudformation/s3-bucket.yaml` and answer:

1. Which resource creates the bucket?
2. Which settings block public access?
3. Which setting enables versioning?
4. Which output returns the bucket ARN?
5. What would you inspect before deploying this template to production?

