# Choose the Right Pipeline Pattern

The right pipeline design follows latency, scale, and recovery needs.

Strong signals:

- batch when latency tolerance is moderate and simpler operations are preferred
- streaming when low-latency reaction matters
- ELT when raw landing and warehouse-side transformation are valuable
- ETL when pre-load transformation control matters

