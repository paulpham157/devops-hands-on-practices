# What a Data Pipeline Is

A data pipeline is not only a scheduled script.

It is the controlled movement of data through stages:

- ingest
- stage or land
- transform
- validate
- publish
- consume

The pipeline becomes trustworthy only when these stages are observable and recoverable.

