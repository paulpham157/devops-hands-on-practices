# Gateway Routing, Aggregation, and Offloading

1. `/checkout`.
2. `mobile-bff`.
3. `tls_termination`, `request_logging`, or `response_compression`.
4. `user-profile-service`, `notifications-service`, and `order-summary-service`.
5. Gateway latency and partial fan-out failure handling become more complex.

