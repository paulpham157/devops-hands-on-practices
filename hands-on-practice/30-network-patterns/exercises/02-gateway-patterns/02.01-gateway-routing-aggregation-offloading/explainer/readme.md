# Gateway Routing, Aggregation, and Offloading

Gateway patterns are related but not identical.

- Routing decides where a request goes
- Aggregation combines responses from multiple backends
- Offloading moves shared edge concerns out of backend services

One gateway can do several of these jobs, but that should be a deliberate choice.

