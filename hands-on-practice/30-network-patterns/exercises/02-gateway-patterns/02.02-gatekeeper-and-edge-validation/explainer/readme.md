# Gatekeeper and Edge Validation

Gatekeeper protects sensitive backends by filtering or rejecting traffic early.

This is useful when obviously invalid or unauthorized requests should never consume backend capacity.

It is not a substitute for all backend validation, but it is a strong first boundary.

