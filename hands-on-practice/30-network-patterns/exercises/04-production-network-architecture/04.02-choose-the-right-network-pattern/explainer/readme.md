# Choose the Right Network Pattern

The right pattern follows the traffic problem.

Strong signals:

- Aggregation when clients are too chatty
- Routing when different capabilities need clear path or host dispatch
- Gatekeeper when invalid traffic must be filtered before backend admission
- Ambassador when outbound access behavior is repeated
- Valet Key when direct temporary resource access is needed
- Static Content Hosting when assets do not need app compute

