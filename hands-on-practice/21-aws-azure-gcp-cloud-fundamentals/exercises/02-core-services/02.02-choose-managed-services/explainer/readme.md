# Explainer: Choose Managed Services

Managed services reduce operational work.

Examples:

- managed database instead of self-managed database on VMs
- managed Kubernetes instead of self-managed control plane
- managed object storage instead of file servers
- managed secret store instead of encrypted files

Managed services are not free of tradeoffs. They can increase provider coupling and cost.

Choose based on operational capacity and business value.

