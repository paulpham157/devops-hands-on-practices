# Solution: Regions, Zones, and Resource Hierarchy

AWS uses accounts as strong isolation and billing boundaries.

Azure uses subscriptions as billing and management boundaries.

Google Cloud uses projects as core isolation, quota, billing, and IAM boundaries.

Multiple zones improve availability because a workload can survive one zone failure.

Separate environments reduce blast radius, simplify access control, and make cost ownership clearer.

