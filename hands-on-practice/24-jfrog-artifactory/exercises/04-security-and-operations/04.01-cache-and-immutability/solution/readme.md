# Cache and Immutability

1. `*-dev-local` repositories.
2. `*-release-local` repositories.
3. To control storage growth and remove unused dependency cache.
4. Production rollback and audit depend on versions resolving to the same artifact.
5. Risks include stale vulnerable dependencies, dependency confusion, and cached malicious packages if policy controls are weak.

