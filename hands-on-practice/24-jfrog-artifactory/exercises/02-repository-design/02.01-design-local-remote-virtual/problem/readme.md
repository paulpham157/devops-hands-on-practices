# Design Local, Remote, and Virtual Repositories

Open `repository-design/repositories.yml`.

Answer:

1. Which Docker repositories are local?
2. Which repository caches Docker Hub?
3. Which Docker repository should developers configure?
4. Why should `docker-release-local` be immutable?
5. Add one missing package ecosystem you would support in a real platform.

