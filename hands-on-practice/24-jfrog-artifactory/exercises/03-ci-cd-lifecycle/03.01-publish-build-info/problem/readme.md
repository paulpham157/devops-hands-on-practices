# Publish Build Info

Open `ci-cd/jfrog-cli-build-info.sh` and `sample-package/build-info.json`.

Answer:

1. Which command uploads the artifact?
2. Which command collects environment metadata?
3. Which command adds Git metadata?
4. Which command publishes build info?
5. Why is build info better than only storing a `.tgz` file?

