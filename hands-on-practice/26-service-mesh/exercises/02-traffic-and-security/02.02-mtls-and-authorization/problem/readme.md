# mTLS and Authorization

Open `istio/peer-authentication.yaml`, `istio/authorization-policy.yaml`, `linkerd/server-policy.yaml`, and `consul/service-intentions.yaml`.

Answer:

1. Which Istio file sets mesh mTLS mode?
2. What mode is configured?
3. Which Istio policy limits callers to the `reviews` workload?
4. Which Linkerd object defines the protected server target?
5. Which Consul object expresses allow or deny between services?

