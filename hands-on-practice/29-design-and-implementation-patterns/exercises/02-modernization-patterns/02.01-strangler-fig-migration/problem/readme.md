# Strangler Fig Migration

Open `strangler-fig/route-map.yml` and `strangler-fig/migration-notes.md`.

Answer:

1. Which capability routes to `checkout-service`?
2. Which paths still fall back to the legacy app?
3. What does the rollback rule do?
4. Why is this safer than a big-bang rewrite?
5. What extra complexity appears during coexistence?

