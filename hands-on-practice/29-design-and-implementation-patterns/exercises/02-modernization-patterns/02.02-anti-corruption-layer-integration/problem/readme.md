# Anti-Corruption Layer Integration

Open `anti-corruption-layer/translation-contract.json` and `anti-corruption-layer/adapter-flow.yml`.

Answer:

1. Which external field maps to `paymentStatus`?
2. What should happen before the domain service receives the command?
3. Why should legacy enum names not be consumed directly by domain services?
4. What is one risk if translation logic is duplicated across many callers?

