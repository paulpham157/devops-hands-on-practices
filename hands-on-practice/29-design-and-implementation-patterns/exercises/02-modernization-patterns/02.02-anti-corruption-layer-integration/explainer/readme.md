# Anti-Corruption Layer Integration

An ACL protects the internal model by translating external contracts at the boundary.

This is especially useful when:

- external naming is poor
- external states do not match domain concepts
- the external API cannot be changed

The pattern is about protecting the internal language, not just renaming fields.

