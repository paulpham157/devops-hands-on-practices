# When to Use Design Patterns

These patterns are valuable when they isolate real architectural pressure.

Good reasons:

- migration must happen gradually
- external model mismatch is damaging the domain
- a repeated runtime concern should not be copied into every service

Bad reasons:

- pattern sounds modern
- the team wants abstraction without a concrete boundary problem

