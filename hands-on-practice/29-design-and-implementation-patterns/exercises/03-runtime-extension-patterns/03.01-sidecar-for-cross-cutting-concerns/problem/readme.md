# Sidecar for Cross-Cutting Concerns

Open `sidecar/pod.yaml` and `sidecar/responsibilities.md`.

Answer:

1. How many containers are in the pod?
2. Which container is the helper?
3. What shared resource lets the helper observe app logs?
4. Why is this concern a better sidecar candidate than business validation logic?

