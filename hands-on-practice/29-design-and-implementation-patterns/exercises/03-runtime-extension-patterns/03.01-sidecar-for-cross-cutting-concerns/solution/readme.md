# Sidecar for Cross-Cutting Concerns

1. Two.
2. `log-forwarder`.
3. The shared `app-logs` volume.
4. Log forwarding is a repeated operational concern, not part of the service's core domain decisions.

