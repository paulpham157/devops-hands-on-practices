# Why BFF

BFF exists when one generic backend shape does not serve all clients well.

Common signals:

- web and mobile need different payloads
- clients make too many backend calls
- client teams release independently
- one shared API forces overfetching or awkward orchestration in the client

