# Aggregation, Transformation, and Auth

1. `user-profile-service`, `orders-service`, and `notifications-service`.
2. Combining responses, normalizing errors, trimming unused fields, or caching safe read data.
3. Session or token adaptation, safe caching, and downstream error normalization.
4. Domain services remain the source of truth and should own business invariants.

