# Problem: Events, Bindings, and Integrations

Read:

```text
scenario-designs/image-resize.yml
scenario-designs/webhook-handler.yml
```

Answer:

1. Why should image resize be asynchronous?
2. Why should webhook handler return quickly?
3. Where should signature validation happen?
4. Why use a queue during traffic spikes?
5. What makes an event handler idempotent?

