# Explainer: Events, Bindings, and Integrations

Serverless platforms are valuable because they connect to events.

Examples:

- storage upload event
- queue message
- scheduled timer
- HTTP request
- database stream
- edge request

Bindings are provider-specific ways to connect functions to data stores, queues, secrets, or services.

Design event payloads and retry behavior before deployment.

