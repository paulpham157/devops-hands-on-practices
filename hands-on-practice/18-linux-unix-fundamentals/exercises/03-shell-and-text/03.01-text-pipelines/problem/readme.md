# Problem: Text Pipelines

Run:

```bash
./scripts/text-pipeline-demo.sh
```

Then write commands to answer:

1. How many `ERROR` lines are in `sample-data/app.log`?
2. How many requests returned status `503`?
3. Which lines mention `service=checkout`?
4. Which command counts total lines in the log?
5. Which pipeline summarizes `sample-data/status-codes.txt`?

