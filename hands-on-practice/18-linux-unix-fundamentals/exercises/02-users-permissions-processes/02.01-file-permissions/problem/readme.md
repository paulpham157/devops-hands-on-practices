# Problem: File Permissions

Run:

```bash
./scripts/permissions-demo.sh
```

Then answer:

1. Why could `run.sh` not be treated as executable before `chmod +x`?
2. What does `chmod 640 config.txt` mean?
3. Which command shows your current user and groups?
4. Why should secrets not be world-readable?
5. Why should production containers avoid running as root?

