# Solution: Compare Protocol Styles

REST is easiest to test with `curl`.

gRPC uses Protobuf in this lesson.

WebSocket is the best fit for bidirectional realtime communication.

Server-Sent Events is a good fit for one-way browser notifications from server to client.

Message-driven APIs decouple producers and consumers through a broker, queue, or event stream.

