# Problem: Contracts and Schemas

Open the contract files:

```text
rest-api/openapi.yaml
grpc-service/proto/catalog.proto
protocol-specs/schema.graphql
protocol-specs/asyncapi.yaml
```

Answer:

1. Which file defines `/products/{productId}`?
2. Which file defines `GetProduct`?
3. Which file defines GraphQL `Query`?
4. Which file defines the `catalog.product.changed` event channel?
5. Why should contracts be reviewed in pull requests?

