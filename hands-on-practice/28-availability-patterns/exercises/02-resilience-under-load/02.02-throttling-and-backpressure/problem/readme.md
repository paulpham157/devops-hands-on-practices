# Throttling and Backpressure

Open `throttling/policy.yml` and `throttling/backpressure-notes.md`.

Answer:

1. What HTTP status code is returned when throttling triggers?
2. What retry guidance is given to the client?
3. Which dependency signal reduces checkout request rate?
4. Why are bounded queues part of backpressure?
5. Why is early rejection often safer than allowing unbounded inflight work?

