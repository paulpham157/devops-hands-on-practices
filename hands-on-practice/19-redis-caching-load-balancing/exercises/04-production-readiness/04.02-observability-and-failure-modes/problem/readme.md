# Problem: Observability and Failure Modes

Read:

```text
notes/failure-modes.md
```

Answer:

1. What should happen if Redis is down but origin is still available?
2. What should happen if one app instance fails?
3. Which metrics show cache effectiveness?
4. Which metrics show Redis memory pressure?
5. Why can load balancer retries make an outage worse?

