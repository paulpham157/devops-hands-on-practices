# Problem: Compare Language Runtimes

Read:

```text
README.md
notes/runtime-comparison.md
```

Answer:

1. Which examples run with an interpreter?
2. Which example runs on a JVM?
3. Which examples compile to a native binary?
4. Which stacks are likely to have the smallest runtime images?
5. Which stack is likely to have the heaviest baseline memory usage?

