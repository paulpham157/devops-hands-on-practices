# Problem: Read Dockerfiles

Inspect every Dockerfile under:

```text
apps/
```

Answer:

1. Which Dockerfiles are single-stage?
2. Which Dockerfiles are multi-stage?
3. Which files are copied first to improve dependency caching?
4. Which images run as a non-root user?
5. Which final images do not include a compiler?

