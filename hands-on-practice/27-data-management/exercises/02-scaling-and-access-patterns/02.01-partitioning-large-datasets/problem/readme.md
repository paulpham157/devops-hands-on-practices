# Partitioning Large Datasets

Open `partitioning/customer-events-partitions.yml`.

Answer:

1. Which partition strategy is used?
2. Which field is the partition key?
3. What is one benefit of hashing on `customer_id`?
4. What is one risk called out in the file?
5. When might range or time partitioning be better than hashing?

