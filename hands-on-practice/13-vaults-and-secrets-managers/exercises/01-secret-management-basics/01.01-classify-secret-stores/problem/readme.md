# Problem: Classify Secret Stores

Read:

```bash
sed -n '1,180p' README.md
```

For each scenario, choose the best starting point:

1. A single Docker Compose lab needs one local token.
2. A production AWS app needs database credentials with managed rotation.
3. A multi-cloud platform team wants dynamic database credentials and audit.
4. A Kubernetes workload needs a token at runtime.
5. A GitOps repo needs encrypted desired state.
6. A GitHub Actions workflow needs a registry token.
