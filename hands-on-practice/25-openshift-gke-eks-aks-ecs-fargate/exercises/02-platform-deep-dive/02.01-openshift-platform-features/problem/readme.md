# OpenShift Platform Features

Open `openshift/route.yaml` and `openshift/platform-features.md`.

Answer:

1. Which object exposes the service externally?
2. What is the destination service name?
3. What OpenShift feature is called out for pod security controls?
4. Why might an enterprise choose OpenShift instead of assembling plain Kubernetes add-ons?
5. What extra learning cost comes with OpenShift?

