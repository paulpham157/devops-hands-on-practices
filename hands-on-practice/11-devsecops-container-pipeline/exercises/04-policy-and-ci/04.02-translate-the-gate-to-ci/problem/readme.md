# Problem: Translate the Gate to CI

Read:

```bash
sed -n '1,220p' ci/github-actions-devsecops.yml
```

Answer:

1. Which event triggers the workflow?
2. Which local script does CI run?
3. Which artifact does the workflow upload?
4. Why is the workflow stored under `ci/` instead of `.github/workflows/` in this course?
