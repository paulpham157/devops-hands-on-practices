# Problem: Build and Scan the Image

Run:

```bash
./scripts/build-image.sh
./scripts/scan-image.sh
```

Answer:

1. What image tag is built by default?
2. Which script mounts the Docker socket?
3. Why does the scanner need access to the Docker socket?
4. Why are image vulnerabilities report-only by default in this lesson?
