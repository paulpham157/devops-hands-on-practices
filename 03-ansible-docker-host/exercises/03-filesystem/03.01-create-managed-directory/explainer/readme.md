# Create Managed Directory

The `file` module manages filesystem state.

Use it instead of shell commands when possible because it is declarative and idempotent. A directory task should state path, type, owner, group, and mode.
