# Split Playbook into Role

Roles package tasks, defaults, handlers, templates, and files into a reusable directory structure.

For a real team, roles make host configuration easier to review and reuse than one long playbook.
