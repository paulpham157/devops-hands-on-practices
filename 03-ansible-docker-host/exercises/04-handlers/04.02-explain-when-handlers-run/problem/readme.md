# Explain When Handlers Run

## Task

Read `site.yml` and answer:

1. Which task should notify the Docker restart handler?
2. When does the handler run in a normal play?
3. Why is that better than restarting Docker after every task?
4. What would you inspect if the handler never ran?

## Done When

You can explain change detection, notification, and delayed handler execution in one short paragraph.
