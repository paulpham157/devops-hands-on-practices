# Add Package and Check Mode

Idempotent automation can run repeatedly without causing unnecessary changes.

Ansible check mode predicts changes without applying them. It is useful before touching shared infrastructure, but it is still a prediction and should be verified.
