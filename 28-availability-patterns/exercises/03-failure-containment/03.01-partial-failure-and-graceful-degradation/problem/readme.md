# Partial Failure and Graceful Degradation

Use `scenario-designs/checkout-spike.yml` and `scenario-designs/dependency-outage.yml`.

Answer:

1. Which optional feature is disabled during the checkout spike scenario?
2. Which core path must continue in the dependency outage scenario?
3. Why is graceful degradation often better than hard failure for optional features?
4. What is one risk if degradation behavior is not planned ahead of time?

