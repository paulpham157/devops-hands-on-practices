# Health Endpoint Monitoring

Open `health-monitoring/health-contract.json` and `health-monitoring/status-policy.yml`.

Answer:

1. Which endpoint should answer whether the instance can receive traffic?
2. Which dependency problem is listed as a readiness failure?
3. Which optional dependency should not fail liveness?
4. Why is it dangerous to put every dependency check into liveness?

