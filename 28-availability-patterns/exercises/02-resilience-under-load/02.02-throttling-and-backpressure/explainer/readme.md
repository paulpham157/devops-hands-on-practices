# Throttling and Backpressure

Throttling protects capacity by rejecting or delaying excess work.

Backpressure makes limits visible instead of silently overcommitting the system.

This is often better than pretending to accept infinite demand and then failing later under timeout storms.

