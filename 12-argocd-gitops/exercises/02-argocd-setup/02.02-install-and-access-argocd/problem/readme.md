# Problem: Install and Access Argo CD

Run:

```bash
./scripts/install-argocd.sh
./scripts/get-admin-password.sh
```

In another terminal:

```bash
./scripts/port-forward-ui.sh
```

Answer:

1. Which namespace contains Argo CD?
2. Which service is port-forwarded for the UI?
3. Which username is used for the first login?
4. Why should production use SSO and RBAC instead of relying on the initial admin account?
