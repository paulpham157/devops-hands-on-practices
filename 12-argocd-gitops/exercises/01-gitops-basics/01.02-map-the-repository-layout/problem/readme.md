# Problem: Map the Repository Layout

Run:

```bash
find gitops-repo argocd -type f | sort
```

Answer:

1. Which directory contains the reusable Kubernetes base?
2. Which directory contains the dev overlay?
3. Which file sets the dev replica count?
4. Which file tells Argo CD where to find the app in Git?
