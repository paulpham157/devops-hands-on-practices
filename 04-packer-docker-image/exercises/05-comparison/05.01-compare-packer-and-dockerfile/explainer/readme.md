# Compare Packer and Dockerfile

Dockerfiles are the common path for container images.

Packer can build Docker images too, but it is especially valuable when the same image-baking workflow needs to produce VM images, cloud images, or other artifacts.
