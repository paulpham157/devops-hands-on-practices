# Avoid Ambiguous Tags in Release Flow

Tags like `latest` are convenient, but they are weak audit evidence.

Repeatable delivery usually needs at least one immutable or versioned tag so teams can tell exactly what was built and deployed.
