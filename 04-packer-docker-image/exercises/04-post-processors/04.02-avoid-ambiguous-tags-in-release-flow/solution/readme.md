# Avoid Ambiguous Tags in Release Flow

A version tag points to one known build, which helps rollback and audit work.

Keeping both a version tag and `latest` is acceptable when automation and humans both know the version tag is the authoritative release reference.
