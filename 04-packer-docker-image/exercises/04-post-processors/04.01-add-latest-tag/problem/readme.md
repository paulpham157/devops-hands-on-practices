# Add Latest Tag

## Task

Add a second local tag named:

```text
docker-course/packer-nginx:latest
```

Build the image and inspect both tags.

## Done When

`docker images docker-course/packer-nginx` shows the version tag and `latest`.
