# Add Latest Tag

Expected result:

```text
docker-course/packer-nginx   1.1
docker-course/packer-nginx   latest
```

Use `latest` only as a convenience in this lab. For repeatable deployment, prefer version tags or image digests.
