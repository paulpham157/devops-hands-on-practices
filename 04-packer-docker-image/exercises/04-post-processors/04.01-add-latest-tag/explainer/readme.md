# Add Latest Tag

Packer post-processors can transform or tag build artifacts after the main build.

Adding a `latest` tag is useful in a local lab, but production pipelines should treat mutable tags carefully.
