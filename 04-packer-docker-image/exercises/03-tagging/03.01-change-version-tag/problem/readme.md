# Change Version Tag

## Task

Change the image tag from `1.0` to `1.1` in the Packer template.

Build and inspect:

```bash
docker images docker-course/packer-nginx
```

## Done When

The local image list shows `docker-course/packer-nginx:1.1`.
