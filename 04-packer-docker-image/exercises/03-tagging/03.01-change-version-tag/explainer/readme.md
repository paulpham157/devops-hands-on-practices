# Change Version Tag

Image tags are references to image content.

A version-like tag such as `1.1` is easier to reason about than a mutable `latest` tag. In production, immutable tags or digests reduce deployment surprise.
