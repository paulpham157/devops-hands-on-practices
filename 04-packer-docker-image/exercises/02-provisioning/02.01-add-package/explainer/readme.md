# Add Package

Packer provisioners run commands while building the image.

Provisioning is where teams install packages, copy files, configure services, and clean temporary build state.

Keep package additions intentional because each package increases image size and maintenance surface.
