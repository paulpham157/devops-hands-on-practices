# Validate Template Before Build

## Task

Run:

```bash
packer fmt -check .
packer validate .
```

Then answer:

1. What does `fmt` help with?
2. What type of mistakes can `validate` catch before build?
3. Why is validation still useful even in a small local image lab?

## Done When

You can explain why validation belongs before `packer build`, not after.
