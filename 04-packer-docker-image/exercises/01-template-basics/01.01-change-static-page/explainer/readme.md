# Change Static Page

Packer builds an image from a template and supporting files.

In this lesson, `files/index.html` is copied into the image. Changing the file requires a new build because the result is baked into the image.
