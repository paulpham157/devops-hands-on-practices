# Promote Artifacts

Run:

```bash
./scripts/simulate-promotion.sh
```

Then inspect `sandbox/promotion-summary.txt`.

Answer:

1. Where was the artifact first published?
2. Which repositories did it move through?
3. Did the script rebuild for release?
4. Why is rebuilding for release risky?
5. Which file represents build metadata?

