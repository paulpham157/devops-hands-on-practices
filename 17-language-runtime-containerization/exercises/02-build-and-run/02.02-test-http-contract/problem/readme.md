# Problem: Test the HTTP Contract

Run:

```bash
docker compose up --build
```

In another terminal:

```bash
./scripts/smoke-test.sh
```

Answer:

1. Which port belongs to Python Flask?
2. Which port belongs to Java Spring?
3. Which port belongs to Ruby Sinatra?
4. Which port belongs to Go HTTP?
5. Which port belongs to Rust HTTP?
6. Why is a shared HTTP contract useful when comparing runtimes?

