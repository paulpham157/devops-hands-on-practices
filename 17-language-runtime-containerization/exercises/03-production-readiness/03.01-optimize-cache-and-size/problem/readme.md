# Problem: Optimize Cache and Size

Inspect the Dockerfiles and answer:

1. Which Dockerfile would rebuild dependencies if only `app.py` changes?
2. Which Dockerfiles keep compilers out of the final image?
3. Why does `pip install --no-cache-dir` matter?
4. Why are Go and Rust runtime images based on distroless images?
5. What extra step would you add before production deployment to compare image sizes?

