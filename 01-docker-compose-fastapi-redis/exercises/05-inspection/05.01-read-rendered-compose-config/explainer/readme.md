# Read Rendered Compose Config

`docker compose config` renders the final model after Compose merges defaults, resolves paths, and validates the file.

Use it before debugging a runtime issue. It shows what Docker will actually receive, not only what you think the YAML says.
