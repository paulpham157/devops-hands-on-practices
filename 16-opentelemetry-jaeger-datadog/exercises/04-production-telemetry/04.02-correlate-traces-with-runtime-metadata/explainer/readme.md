# Correlate Traces with Runtime Metadata

A trace is easier to use when it carries resource metadata.

Attributes such as service name, environment, version, and region help connect a slow trace to the deployment that produced it. Without that context, traces can show symptoms but not ownership.
