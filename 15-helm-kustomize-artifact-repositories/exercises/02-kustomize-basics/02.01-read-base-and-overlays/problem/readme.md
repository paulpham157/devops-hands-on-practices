# Problem: Read Base and Overlays

Inspect:

```text
kustomize/base/
kustomize/overlays/dev/
kustomize/overlays/prod/
```

Answer:

1. Which resources are defined in the base?
2. Which overlay changes the image tag?
3. Which overlay changes replica count?
4. Which overlay changes resource requests and limits?
5. Why is Kustomize useful when the base YAML should stay readable?

