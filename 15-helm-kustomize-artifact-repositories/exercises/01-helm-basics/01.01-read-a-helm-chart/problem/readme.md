# Problem: Read a Helm Chart

Inspect `charts/course-web/`.

Answer these questions:

1. What Kubernetes resources does the chart create?
2. Which values control the image repository and tag?
3. Which values are different between dev and prod?
4. What does `values.schema.json` protect against?
5. Why does the chart include `_helpers.tpl` instead of repeating labels everywhere?

