# Problem: NGINX Round-Robin and Least Connections

Open:

```text
nginx/default.conf
```

Answer:

1. What is the upstream group name?
2. Which backend services are in the group?
3. Which load balancing strategy is configured?
4. What happens if you remove `least_conn`?
5. Why can least-connections help when requests have uneven duration?

