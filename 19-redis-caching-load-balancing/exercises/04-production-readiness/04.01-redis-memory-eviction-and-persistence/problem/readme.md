# Problem: Redis Memory, Eviction, and Persistence

Open:

```text
redis/redis.conf
```

Answer:

1. What is the memory limit?
2. What eviction policy is configured?
3. What does `appendonly yes` enable?
4. When is `allkeys-lru` reasonable?
5. When would `noeviction` be safer?

