# Problem: Cache-Aside and TTL

Run:

```bash
./scripts/start-stack.sh
./scripts/cache-demo.sh
```

Answer:

1. Which request is a cache miss?
2. Which request is a cache hit?
3. Which field shows the remaining TTL?
4. Which field shows simulated origin latency?
5. Why should most cache keys have TTLs?

