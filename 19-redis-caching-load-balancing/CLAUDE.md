# Claude Instructions

Read `AGENTS.md` in this lesson directory first, then read the root `../AGENTS.md` if broader repository guidance is needed.

Treat `19-redis-caching-load-balancing/AGENTS.md` as the source of truth for lesson-specific structure, commands, validation, and editing rules. Do not duplicate those rules here.
