# Extend Terraform with Modules

Open `terraform/main.tf` and `terraform/modules/tagged-s3-bucket`.

Answer:

1. Which input controls the environment tag?
2. Which input lets callers add team-specific tags?
3. Which resources are hidden behind the module?
4. What new variable would you add to make versioning configurable?
5. Why is a module safer than copy-pasting the same resources across services?

