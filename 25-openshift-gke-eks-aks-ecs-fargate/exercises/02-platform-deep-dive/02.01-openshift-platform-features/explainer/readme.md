# OpenShift Platform Features

OpenShift is useful when the organization wants more than vanilla Kubernetes hosting.

The platform adds operational opinion through features such as:

- Routes
- Security Context Constraints
- Operators

This means OpenShift can reduce platform assembly work, but teams must learn OpenShift-specific objects and operational patterns.

