# ECS and Fargate Task Design

Open `ecs/task-definition.json` and `ecs/workload-model.md`.

Answer:

1. Which field shows this task is designed for Fargate?
2. Which IAM role is used by the running application container?
3. Which field configures logging?
4. What ECS concept replaces a Kubernetes Deployment for long-running tasks?
5. Why might an AWS-only team prefer this model over Kubernetes?

