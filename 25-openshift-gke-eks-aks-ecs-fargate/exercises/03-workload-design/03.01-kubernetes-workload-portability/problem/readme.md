# Kubernetes Workload Portability

Open `kubernetes-manifests/deployment.yaml` and `kubernetes-manifests/service.yaml`.

Answer:

1. Which Kubernetes object controls replica count?
2. Which object exposes the workload internally?
3. Which fields show a baseline security posture?
4. Why is this manifest more portable than a provider-specific ingress example?
5. Which platforms in this lesson could run this workload model directly?

