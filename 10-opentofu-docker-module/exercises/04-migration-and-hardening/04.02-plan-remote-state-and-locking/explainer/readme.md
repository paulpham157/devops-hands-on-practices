# Plan Remote State and Locking

Single-user local state is fine for a learning lab, but shared infrastructure needs stronger controls.

Remote state and locking reduce the chance that two operators plan or apply against stale assumptions at the same time.
