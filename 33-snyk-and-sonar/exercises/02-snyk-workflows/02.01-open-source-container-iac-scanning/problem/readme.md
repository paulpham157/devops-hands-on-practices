# Open Source, Container, and IaC Scanning

Open `snyk/cli-workflow.sh` and `snyk/sample-results.json`.

Answer:

1. Which command scans open source dependencies?
2. Which command scans a container image?
3. Which command scans IaC?
4. How many critical container findings appear in the sample result?
5. What example IaC finding is listed?

