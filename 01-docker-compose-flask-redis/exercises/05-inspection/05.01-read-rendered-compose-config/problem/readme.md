# Read Rendered Compose Config

## Task

Run:

```bash
docker compose config
```

Find:

1. The final web image build context.
2. The published port mapping.
3. The Redis service name.
4. Any named volumes.

## Done When

You can explain how the rendered config maps to the running containers.
