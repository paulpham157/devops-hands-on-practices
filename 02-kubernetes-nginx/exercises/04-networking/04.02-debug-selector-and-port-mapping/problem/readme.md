# Debug Selector and Port Mapping

## Task

Read `manifests/03-deployment.yaml` and `manifests/04-service.yaml`, then answer:

1. Which labels must match for the Service to find Pods?
2. Which Service port is exposed to clients?
3. Which container port does the Service forward to?
4. What symptom would you expect if the selector matched nothing?
5. What symptom would you expect if the target port was wrong?

## Done When

You can explain how to debug a Service that exists but does not serve traffic.
