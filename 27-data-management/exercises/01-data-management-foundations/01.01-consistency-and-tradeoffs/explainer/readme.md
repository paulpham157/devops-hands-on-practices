# Consistency and Tradeoffs

Distributed data systems rarely get every property at once.

The key question is not "do we want consistency?" The real question is:

- where is strict consistency required
- where is eventual consistency acceptable
- what product behavior is acceptable during propagation delay

Different domains need different answers.

