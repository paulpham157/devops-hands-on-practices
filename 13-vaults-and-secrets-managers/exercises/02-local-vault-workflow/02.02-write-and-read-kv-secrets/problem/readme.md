# Problem: Write and Read KV Secrets

Run:

```bash
./scripts/write-vault-secret.sh
./scripts/read-vault-secret.sh
```

Answer:

1. Which Vault path stores the secret?
2. Which command writes the secret?
3. Which fields are stored?
4. Why should application code avoid logging those fields?
