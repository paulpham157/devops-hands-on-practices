# Gateway Routing, Aggregation, and Offloading

Open `gateway-patterns/route-map.yml` and `gateway-patterns/aggregation-flow.yml`.

Answer:

1. Which path routes to `checkout-service`?
2. Which target handles `mobile-api.example.internal`?
3. Name two concerns being offloaded at the gateway.
4. Which backend calls are combined for the dashboard?
5. What is one risk of aggregation at the gateway?

