# Gatekeeper and Edge Validation

1. `payments-admin-api`.
2. `tenant_header`.
3. Over 1 MB is rejected.
4. `403`.
5. It rejects obviously bad or unauthorized requests before they consume protected backend capacity.

