# Gatekeeper and Edge Validation

Open `gatekeeper/policy.yml`.

Answer:

1. Which protected service is listed?
2. Which header is required?
3. What payload size limit is enforced?
4. What status code is returned on block?
5. Why is this useful before requests reach a sensitive admin API?

