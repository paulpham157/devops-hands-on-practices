# Choose the Right Network Pattern

Choose the strongest first pattern for each case.

1. Mobile client must fetch one dashboard from several backends.
2. Many services call the same external partner API with identical auth and retry behavior.
3. Authenticated users should download large files without making app servers stream every byte.
4. Admin API should reject bad headers and oversized payloads before backend processing.

Explain one tradeoff for each choice.

