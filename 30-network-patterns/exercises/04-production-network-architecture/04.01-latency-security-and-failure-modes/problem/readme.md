# Latency, Security, and Failure Modes

Use `pattern-maps/operations-checklist.md`.

Answer:

1. Why can aggregation reduce client latency while increasing gateway latency?
2. Why must valet-key tokens be scoped and short-lived?
3. Why should ambassador failure be monitored separately from application success logs?
4. Why can a gateway become a bottleneck if too many concerns are offloaded there?

