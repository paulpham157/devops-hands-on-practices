# Latency, Security, and Failure Modes

Network patterns are useful only if their failure modes are understood.

Questions to ask:

- does the gateway add too much latency?
- what happens if the aggregation backend partially fails?
- can the token issuer for valet key become a bottleneck?
- does the ambassador become a hidden dependency?

