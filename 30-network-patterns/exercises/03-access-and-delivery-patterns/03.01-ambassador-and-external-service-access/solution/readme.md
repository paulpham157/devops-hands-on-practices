# Ambassador and External Service Access

1. `local-ambassador`.
2. `partner-billing-api`.
3. Adding external auth headers, retrying transient failures, or translating the local endpoint to the remote API.
4. It centralizes repeated outbound behavior and keeps application code simpler and more consistent.

