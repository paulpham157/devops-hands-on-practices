# Ambassador Responsibilities

The Ambassador pattern is strongest when many workloads need the same outbound access behavior.

Typical tasks:

- external auth decoration
- proxying to remote APIs
- protocol normalization
- outbound retry and timeout policy

It should not become a hidden place for business decisions.

