# Correlate Metrics and Logs

Metrics show that something changed. Logs often explain what changed.

A practical incident workflow starts with a dashboard symptom, drills into the exact time window, then searches logs for the same service and status pattern.

The goal is not to stare at every log line. The goal is to narrow the search with labels, time range, and known symptoms.
