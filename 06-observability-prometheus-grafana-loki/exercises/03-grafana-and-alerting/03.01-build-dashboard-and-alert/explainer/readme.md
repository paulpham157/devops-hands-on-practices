# Build a Dashboard and Alert

A useful dashboard connects symptoms to likely causes.

For this sample app, the dashboard should answer:

- Is traffic arriving?
- Is the app failing?
- Is latency high?
- Which endpoint/status is responsible?
- What do the related logs say?

Prometheus handles metric panels and alert rules. Loki handles log panels. Grafana brings both into one workflow.

